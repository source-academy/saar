import React from 'react';
type Props = {
    isSelected: boolean;
    isInFront: boolean;
};
/**
 * Outline for ARObject.
 * Currently only works with UI and shape objects.
 */
export declare function Outline(props: Props): React.JSX.Element;
export {};
